# Backend Integration Contract

> Auto-generated by the vibe-to-prod skill. This document lists every API endpoint the frontend expects, plus UI primitives ready for design system replacement. Backend team: implement the endpoints. Frontend dev: swap the flagged primitives with your design system components.

## How to read this document

Each endpoint entry corresponds to a function in `src/api.ts` (or `src/api.js` for JavaScript codebases) annotated with `// @backend`. The frontend currently uses mock data and simulated latency. Replace each stub with a real API call matching this contract.

---

## Authentication

| Requirement | Value |
|-------------|-------|
| Auth method | `[Bearer token / Cookie / API key — fill in]` |
| Token header | `Authorization: Bearer <token>` |
| Refresh flow | `[Describe or link to auth docs]` |

---

## Endpoints

### `[EntityName]` — `[brief description]`

| Field | Value |
|-------|-------|
| Method | `GET / POST / PUT / PATCH / DELETE` |
| Path | `/api/v1/[resource]` |
| Auth required | Yes / No |
| Frontend stub | `src/api.ts → functionName()` |

**Request payload:**

```typescript
interface RequestBody {
  // paste from domain.ts
}
```

**Response:**

```typescript
interface Response {
  // expected shape
}
```

**Error cases:**

| Status | Meaning | Frontend handling |
|--------|---------|-----------------|
| 400 | Validation error | Show field-level errors |
| 401 | Unauthorized | Redirect to login |
| 404 | Not found | Show empty state |
| 500 | Server error | Toast notification |

---

*Repeat the "Endpoints" section for each `// @backend` annotation in `src/api.ts` or `src/api.js`.*

---

## Replaced UI Primitives

> These custom vibe-coded components were replaced with shadcn/ui or Radix UI equivalents during the handoff refactor. Each one has a clean prop interface ready for your design system swap. Replace the shadcn import with your Storybook/design system component — no logic changes required.

| Component | Replaced With | File | Props Interface | Notes |
|-----------|--------------|------|-----------------|-------|
| `[CustomDropdown]` | `shadcn/ui <Select />` | `src/components/ui/Select.tsx` | `{ value, onValueChange, options, disabled? }` | Preserves designer's trigger styling via `SelectTrigger` className |
| `[CustomModal]` | `shadcn/ui <Dialog />` | `src/components/ui/Dialog.tsx` | `{ open, onOpenChange, children }` | Focus trap + scroll lock handled by Radix |
| `[CustomTooltip]` | `shadcn/ui <Tooltip />` | `src/components/ui/Tooltip.tsx` | `{ content, children, side? }` | Positioning handled by Radix Popper |

*Add one row per replaced primitive. Remove this table if no primitives were replaced.*

---

## Preserved Custom Components

> These components were built custom by the designer and contain genuine product-specific interactions or visualizations. They are not candidates for design system replacement — integrate them as-is.

| Component | File | Description |
|-----------|------|-------------|
| `[ComponentName]` | `src/components/[path]` | `[Why it's custom — e.g., "Custom drag-to-reorder timeline unique to this product"]` |

*Add one row per preserved custom component. Remove this table if none exist.*

---

## Flagged Interactions

> These animations or interactions appeared potentially buggy during the refactor (not clearly intentional design). Review before shipping.

| Location | File | Issue | Recommended Action |
|----------|------|-------|--------------------|
| `[ComponentName]` | `src/components/[path]` | `[e.g., "Hover state flickers on rapid mouse-over"]` | `[e.g., "Check CSS transition specificity or debounce handler"]` |

*Remove this table if no issues were flagged.*

---

## Environment Variables

These must be provided for the frontend to connect to real APIs. See `.env.example`.

| Variable | Purpose | Example |
|----------|---------|---------|
| `VITE_API_BASE_URL` | Backend API root | `https://api.example.com/v1` |
| `VITE_AUTH_DOMAIN` | Auth provider domain | `auth.example.com` |

---

## Integration Checklist

```
Frontend dev:
- [ ] Replaced primitives swapped with design system components (see table above)
- [ ] Flagged interactions reviewed and resolved
- [ ] .env variables configured

Backend team:
- [ ] All endpoints implemented and returning correct shapes
- [ ] Auth flow working (login → token → authenticated requests)
- [ ] Error responses match the documented status codes
- [ ] CORS configured for frontend origin
- [ ] Environment variables provided to frontend team

Shared:
- [ ] End-to-end smoke test passing
```